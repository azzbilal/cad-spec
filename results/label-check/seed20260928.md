# Label check: 30 random failed answers (seed 20260928)

Scorer 0.4.0. Pool: 1318 failed answers with judgement labels (mechanical failures such as API errors and syntax errors are left out).

For each: does the LABEL name what the code and measurements show? The correct hole centres are worked out for you: (+/- pitch_x / 2, +/- pitch_y / 2). A plate whose X or Y range is not centred on 0 has been moved.

## 1. no holes
- model: qwen/qwen3-coder-30b-a3b-instruct  tier: L3  spec: gen-0021  run: 20260924T004105Z
- spec: plate 84.0 x 34.5 x 6.5, hole 4.0, pitch 60.0 x 10.5, margin 12.0
- correct hole centres: (+/-30, +/-5.25)
- plate position: X -42 .. 42   Y -17.25 .. 17.25   Z -3.25 .. 3.25
- measured hole centres: []

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 6.5 mm plate cut to 34.5 mm wide (Y) and 84 mm long (X). It needs four 4 mm holes straight through, spaced 60 mm apart along the length and 10.5 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define dimensions
plate_width = 34.5  # Y direction
plate_length = 84   # X direction
hole_diameter = 4
hole_spacing_x = 60
hole_spacing_y = 10.5

# Create the base plate
result = cq.Workplane("XY").box(plate_length, plate_width, 6.5)

# Create holes
# Hole positions centered on the plate
hole_positions = [
    (-hole_spacing_x/2, -hole_spacing_y/2),  # Bottom left
    (hole_spacing_x/2, -hole_spacing_y/2),   # Bottom right
    (-hole_spacing_x/2, hole_spacing_y/2),   # Top left
    (hole_spacing_x/2, hole_spacing_y/2)     # Top right
]

# Add holes to the plate
for x, y in hole_positions:
    result = result.faces(">X").workplane(offset=3.25).hole(hole_diameter)

# Since we're working with a solid, we need to make sure the holes go through
# The hole command with offset should create through holes
# Let's rework this to be more explicit about through holes
result = cq.Workplane("XY").box(plate_length, plate_width, 6.5)
for x, y in hole_positions:
    result = result.faces(">X").workplane(offset=3.25).hole(hole_diameter)

# Alternative approach - create holes in the correct positions
result = cq.Workplane("XY").box(plate_length, plate_width, 6.5)
for x, y in hole_positions:
    result = result.workplane(offset=3.25).center(x, y).hole(hole_diameter)

# Make sure we're cutting through the entire thickness
result = cq.Workplane("XY").box(plate_length, plate_width, 6.5)
for x, y in hole_positions:
    result = result.faces(">X").workplane(offset=3.25).hole(hole_diameter)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 2. CadQuery API error
- model: microsoft/phi-4  tier: L3  spec: gen-0021  run: 20260924T074549092729Z-aa3b44
- spec: plate 84.0 x 34.5 x 6.5, hole 4.0, pitch 60.0 x 10.5, margin 12.0
- correct hole centres: (+/-30, +/-5.25)
- API error kind: no such method: Workplane.centered
- error: execution failed [raised in model code]: AttributeError: 'Workplane' object has no attribute 'centered'

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 6.5 mm plate cut to 34.5 mm wide (Y) and 84 mm long (X). It needs four 4 mm holes straight through, spaced 60 mm apart along the length and 10.5 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions
plate_width = 34.5
plate_length = 84
plate_thickness = 6.5

# Define hole parameters
hole_diameter = 4
hole_spacing_length = 60
hole_spacing_width = 10.5

# Create the plate
plate = cq.Workplane("XY").box(plate_length, plate_width, plate_thickness)

# Calculate the center of the plate
center_x = plate_length / 2
center_y = plate_width / 2

# Calculate the starting positions for the holes
start_x = center_x - (hole_spacing_length / 2)
start_y = center_y - (hole_spacing_width / 2)

# Create the holes
holes = plate.faces(">Z").workplane(). \
    centered(start_x, start_y). \
    hole(hole_diameter). \
    centered(start_x, start_y + hole_spacing_width). \
    hole(hole_diameter). \
    centered(start_x + hole_spacing_length, start_y). \
    hole(hole_diameter). \
    centered(start_x + hole_spacing_length, start_y + hole_spacing_width). \
    hole(hole_diameter)

# Assign the result to the variable
result = holes
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 3. holes stacked at one point
- model: google/gemma-3-27b-it  tier: L2  spec: gen-0028  run: 20260924T021321Z
- spec: plate 70.0 x 47.5 x 5.5, hole 8.5, pitch 37.0 x 14.5, margin 16.5
- correct hole centres: (+/-18.5, +/-7.25)
- plate position: X -35 .. 35   Y -23.75 .. 23.75   Z 0 .. 5.5
- measured hole centres: [(0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        70.0 mm
  R2  Overall width (Y):         47.5 mm
  R3  Plate thickness (Z):       5.5 mm
  R4  Fastener holes:            4 off, through, 8.5 mm diameter
  R5  Hole pattern:              rectangular, one hole near each corner
  R6  Edge margin:               16.5 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

result = (
    cq.Workplane("XY")
    .rect(70.0, 47.5)
    .extrude(5.5)
    .faces(">Z")
    .workplane()
    .center(0, 0)
    .hole(8.5)
    .translate((16.5, 16.5))
    .hole(8.5)
    .translate((-16.5, 16.5))
    .hole(8.5)
    .translate((-16.5, -16.5))
    .hole(8.5)
    .translate((16.5, -16.5))
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 4. no holes
- model: meta-llama/llama-4-maverick  tier: L0  spec: gen-0012  run: 20260924T032942Z
- spec: plate 66.0 x 124.0 x 9.0, hole 10.5, pitch 46.0 x 104.0, margin 10.0
- correct hole centres: (+/-23, +/-52)
- plate position: X -33 .. 33   Y -62 .. 62   Z -4.5 .. 4.5
- measured hole centres: []

<details><summary>full prompt</summary>

````
Fill in the CadQuery template below so the part meets every requirement.

REQUIREMENTS
  R1  Overall length (X):        66.0 mm
  R2  Overall width (Y):         124.0 mm
  R3  Plate thickness (Z):       9.0 mm
  R4  Fastener holes:            4 off, through, 10.5 mm diameter
  R5  Hole pattern:              rectangular, 46.0 mm x 104.0 mm centres
  R6  Edge margin:               10.0 mm from hole centre to each nearest edge

TEMPLATE (replace each ??? with the correct number; change nothing else):

import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(???, ???, ???)
    .faces(">Z").workplane()
    .rect(???, ???, forConstruction=True)
    .vertices()
    .hole(???)
)

box() takes (length_x, width_y, thickness_z). rect() takes the hole pattern
centre distances (pitch_x, pitch_y). hole() takes a DIAMETER, not a radius.
The plate is centred on the origin.

The completed template is the ENTIRE answer: a runnable Python module, with
`import` and `result` at column zero exactly as shown above. Substitute the
numbers and change nothing else. Do not add loops, extra holes, extra
features, comments, or any further operations after the closing parenthesis.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(66.0, 124.0, 9.0)
    .faces(">Z").workplane()
    .rect(104.0, 46.0, forConstruction=True)
    .vertices()
    .hole(10.5)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 5. CadQuery API error
- model: google/gemma-3-4b-it  tier: L3  spec: gen-0208  run: 20260924T145836816612Z-9a9e12
- spec: plate 43.0 x 36.0 x 6.5, hole 8.5, pitch 29.0 x 22.0, margin 7.0
- correct hole centres: (+/-14.5, +/-11)
- API error kind: no such method: Workplane.wrapped
- error: execution failed [raised in model code]: AttributeError: 'Workplane' object has no attribute 'wrapped'

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 6.5 mm plate cut to 36 mm wide (Y) and 43 mm long (X). It needs four 8.5 mm holes straight through, spaced 29 mm apart along the length and 22 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

l = 43.0
w = 36.0
h = 6.5
hole_diameter = 8.5
hole_spacing_length = 29.0
hole_spacing_width = 22.0

result = (
    cq.Workplane("XY")
    .box(l, w, h)
    .faces(">Z")
    .wrapped(
        cq.Workplane("XY")
        .center(l/2, w/2)
        .hole(hole_diameter, (0, 0), (0, 0), (0, 0))
        .hole(hole_diameter, (0, 0), (0, 0), (0, 0))
        .hole(hole_diameter, (0, 0), (0, 0), (0, 0))
        .hole(hole_diameter, (0, 0), (0, 0), (0, 0))
    )
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 6. change not propagated to pitch
- model: openai/gpt-4o-mini  tier: L4  spec: gen-0057  run: 20260924T032121Z
- spec: plate 214.5 x 126.5 x 7.0, hole 4.5, pitch 175.5 x 87.5, margin 19.5
- correct hole centres: (+/-87.75, +/-43.75)
- rev A (before the change): plate 214.5 x 152.0 x 7.0, hole 4.5, pitch 175.5 x 113.0, margin 19.5

<details><summary>full prompt</summary>

````
Below is the current CadQuery model of a mounting plate (rev A).

```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(214.5, 152.0, 7.0)
    .faces(">Z").workplane()
    .rect(175.5, 113.0, forConstruction=True)
    .vertices()
    .hole(4.5)
)
```

ENGINEERING CHANGE ORDER, rev A -> rev B:
  - change overall width (Y) from 152 mm to 126.5 mm
Every other characteristic stays exactly as in rev A: four through holes, one per corner, each hole centre at the stated edge margin from its two nearest edges, part centred on the origin.

Return the complete rev B CadQuery module with the part bound to `result`.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(214.5, 126.5, 7.0)
    .faces(">Z").workplane()
    .rect(175.5, 113.0, forConstruction=True)
    .vertices()
    .hole(4.5)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 7. change not propagated to pitch
- model: mistralai/codestral-2508  tier: L4  spec: gen-0160  run: 20260924T034121Z
- spec: plate 70.5 x 139.5 x 4.0, hole 5.0, pitch 43.5 x 112.5, margin 13.5
- correct hole centres: (+/-21.75, +/-56.25)
- rev A (before the change): plate 60.0 x 139.5 x 4.0, hole 5.0, pitch 33.0 x 112.5, margin 13.5

<details><summary>full prompt</summary>

````
Below is the current CadQuery model of a mounting plate (rev A).

```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(60.0, 139.5, 4.0)
    .faces(">Z").workplane()
    .rect(33.0, 112.5, forConstruction=True)
    .vertices()
    .hole(5.0)
)
```

ENGINEERING CHANGE ORDER, rev A -> rev B:
  - change overall length (X) from 60 mm to 70.5 mm
Every other characteristic stays exactly as in rev A: four through holes, one per corner, each hole centre at the stated edge margin from its two nearest edges, part centred on the origin.

Return the complete rev B CadQuery module with the part bound to `result`.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(70.5, 139.5, 4.0)
    .faces(">Z").workplane()
    .rect(33.0, 112.5, forConstruction=True)
    .vertices()
    .hole(5.0)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 8. geometry kernel failure
- model: qwen2.5-coder:1.5b  tier: L3  spec: gen-0149  run: 20260923T164833Z
- spec: plate 43.0 x 76.5 x 5.0, hole 9.5, pitch 17.0 x 50.5, margin 13.0
- correct hole centres: (+/-8.5, +/-25.25)
- error: execution failed [raised in cadquery: Solid.makeBox]: Standard_DomainError: 

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 43 x 76.5 x 5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.75 mm. Each bore axis lies 13 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions of the part
envelope_x = 43
envelope_y = 76.5
envelope_z = 5
center_x = 0
center_y = 0
center_z = 0
bore_radius = 4.75
bore_distance = 13

# Create the workplane
workplane = cq.Workplane("XY")

# Create the outer envelope
outer_envelope = workplane.box(envelope_x, envelope_y, envelope_z)

# Create the inner envelope
inner_envelope = workplane.box(envelope_x - 2 * bore_distance, envelope_y - 2 * bore_distance, envelope_z - 2 * bore_distance)

# Create the bore holes
bore_holes = []
for i in range(4):
    bore_hole = workplane.circle(bore_radius).translate((bore_distance * i, 0, 0))
    bore_holes.append(bore_hole)

# Combine the outer and inner envelopes with the bore holes
result = outer_envelope - inner_envelope - bore_holes

# Display the result
cq.exporters.export(result, "part.stl")
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 9. gate: simple_through_holes
- model: meta-llama/llama-3.3-70b-instruct  tier: L3  spec: gen-0093  run: 20260924T014436Z
- spec: plate 52.5 x 58.5 x 6.0, hole 12.5, pitch 24.5 x 30.5, margin 14.0
- correct hole centres: (+/-12.25, +/-15.25)
- plate position: X -26.25 .. 26.25   Y -29.25 .. 29.25   Z -3 .. 3
- measured hole centres: [(-12.25, -15.25), (-12.25, 15.25), (12.25, -15.25), (12.25, 15.25)]

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 52.5 x 58.5 x 6 mm, centred at (0, 0, 0). Qty 4 through bores of radius 6.25 mm. Each bore axis lies 14 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

l, w, h = 52.5, 58.5, 6
bore_radius = 6.25
bore_distance = 14

result = (
    cq.Workplane("XY")
   .box(l, w, h)
   .faces(">Z")
   .fillet(0.1)
   .faces("<Z")
   .fillet(0.1)
    - cq.Workplane("XY")
   .moveTo(-l/2 + bore_distance, -w/2 + bore_distance)
   .circle(bore_radius)
   .extrude(h)
    - cq.Workplane("XY")
   .moveTo(l/2 - bore_distance, -w/2 + bore_distance)
   .circle(bore_radius)
   .extrude(h)
    - cq.Workplane("XY")
   .moveTo(-l/2 + bore_distance, w/2 - bore_distance)
   .circle(bore_radius)
   .extrude(h)
    - cq.Workplane("XY")
   .moveTo(l/2 - bore_distance, w/2 - bore_distance)
   .circle(bore_radius)
   .extrude(h)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 10. holes misplaced (other)
- model: mistralai/codestral-2508  tier: L3  spec: gen-0149  run: 20260924T034121Z
- spec: plate 43.0 x 76.5 x 5.0, hole 9.5, pitch 17.0 x 50.5, margin 13.0
- correct hole centres: (+/-8.5, +/-25.25)
- plate position: X -21.5 .. 21.5   Y -38.25 .. 38.25   Z -5 .. 0
- measured hole centres: [(-13.0, -26.75), (0.0, 0.0)]

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 43 x 76.5 x 5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.75 mm. Each bore axis lies 13 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Create the base envelope
result = cq.Workplane("XY").box(43, 76.5, 5)

# Calculate the positions for the through bores
positions = [
    (-13, -26.75),  # Top-left
    (13, -26.75),   # Top-right
    (-13, 26.75),   # Bottom-left
    (13, 26.75)     # Bottom-right
]

# Create the through bores
for pos in positions:
    result = result.faces(">Z").workplane().center(pos[0], pos[1]).hole(9.5)

# Center the part at (0, 0, 0)
result = result.translate((0, 0, -2.5))
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 11. some holes right, some wrong
- model: qwen/qwen3-30b-a3b-instruct-2507  tier: L3  spec: gen-0166  run: 20260924T002523Z
- spec: plate 124.0 x 95.5 x 14.0, hole 11.0, pitch 100.0 x 71.5, margin 12.0
- correct hole centres: (+/-50, +/-35.75)
- plate position: X -62 .. 62   Y -47.75 .. 47.75   Z -7 .. 7
- measured hole centres: [(-50.0, -35.75), (0.0, 0.0)]

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 14 mm plate cut to 95.5 mm wide (Y) and 124 mm long (X). It needs four 11 mm holes straight through, spaced 100 mm apart along the length and 71.5 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define dimensions
plate_width = 95.5  # Y direction
plate_length = 124   # X direction
plate_thickness = 14

# Hole parameters
hole_diameter = 11
hole_spacing_x = 100  # distance between holes along X
hole_spacing_y = 71.5  # distance between holes along Y

# Center the plate on the origin
center_x = plate_length / 2
center_y = plate_width / 2

# Create the plate
plate = cq.Workplane("XY").box(plate_length, plate_width, plate_thickness)

# Define hole positions (centered on origin)
# Hole pattern: 2x2 grid, centered
hole_positions = [
    (-hole_spacing_x / 2, -hole_spacing_y / 2),
    (hole_spacing_x / 2, -hole_spacing_y / 2),
    (-hole_spacing_x / 2, hole_spacing_y / 2),
    (hole_spacing_x / 2, hole_spacing_y / 2)
]

# Add holes through the plate
result = plate
for pos in hole_positions:
    result = result.faces(">Z").workplane().center(pos[0], pos[1]).hole(hole_diameter)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 12. some holes right, some wrong
- model: qwen/qwen3-235b-a22b-2507  tier: L3  spec: gen-0026  run: 20260924T005722Z
- spec: plate 210.5 x 141.5 x 4.5, hole 11.5, pitch 190.5 x 121.5, margin 10.0
- correct hole centres: (+/-95.25, +/-60.75)
- plate position: X -105.25 .. 105.25   Y -70.75 .. 70.75   Z -2.25 .. 2.25
- measured hole centres: [(0.0, 0.0), (95.25, 60.75)]

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 210.5 x 141.5 x 4.5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 5.75 mm. Each bore axis lies 10 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define envelope dimensions
length = 210.5
width = 141.5
height = 4.5

# Bore radius
bore_radius = 5.75
bore_depth = height  # Through bore

# Distance from each bore axis to the two closest edges
edge_distance = 10.0

# Calculate bore center positions
x_centers = [length/2 - edge_distance, -(length/2 - edge_distance)]
y_centers = [width/2 - edge_distance, -(width/2 - edge_distance)]

# Create the base plate
plate = cq.Workplane("XY").box(length, width, height, centered=True)

# Add the four through bores
for x in x_centers:
    for y in y_centers:
        plate = plate.workplane(offset=height/2).center(x, y).hole(bore_radius * 2, depth=None)

result = plate
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 13. pattern anchored at a corner
- model: google/gemma-3-27b-it  tier: L2  spec: gen-0021  run: 20260924T021321Z
- spec: plate 84.0 x 34.5 x 6.5, hole 4.0, pitch 60.0 x 10.5, margin 12.0
- correct hole centres: (+/-30, +/-5.25)
- plate position: X -42 .. 42   Y -17.25 .. 17.25   Z -3.25 .. 3.25
- measured hole centres: [(-30.0, 5.25), (0.0, 0.0), (0.0, 10.5), (30.0, 5.25)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        84.0 mm
  R2  Overall width (Y):         34.5 mm
  R3  Plate thickness (Z):       6.5 mm
  R4  Fastener holes:            4 off, through, 4.0 mm diameter
  R5  Hole pattern:              rectangular, one hole near each corner
  R6  Edge margin:               12.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Requirements
length = 84.0
width = 34.5
thickness = 6.5
hole_diameter = 4.0
edge_margin = 12.0

# Calculate hole positions
hole_x_offset = (length / 2) - edge_margin
hole_y_offset = (width / 2) - edge_margin

hole_positions = [
    (hole_x_offset, hole_y_offset),
    (-hole_x_offset, hole_y_offset),
    (-hole_x_offset, -hole_y_offset),
    (hole_x_offset, -hole_y_offset),
]

# Create the plate
plate = cq.Workplane("XY").box(length, width, thickness)

# Create the holes
for x, y in hole_positions:
    plate = plate.faces(">Z").workplane().center(x, y).hole(hole_diameter)

# Result
result = plate
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 14. holes misplaced (other)
- model: microsoft/phi-4  tier: L1  spec: gen-0041  run: 20260924T030042Z
- spec: plate 86.0 x 61.0 x 8.0, hole 11.0, pitch 63.0 x 38.0, margin 11.5
- correct hole centres: (+/-31.5, +/-19)
- plate position: X -43 .. 43   Y -30.5 .. 30.5   Z -4 .. 4
- measured hole centres: [(0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        86.0 mm
  R2  Overall width (Y):         61.0 mm
  R3  Plate thickness (Z):       8.0 mm
  R4  Fastener holes:            4 off, through, 11.0 mm diameter
  R5  Hole pattern:              rectangular, 63.0 mm x 38.0 mm centres
  R6  Edge margin:               11.5 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions based on the requirements
overall_length = 86.0  # R1
overall_width = 61.0    # R2
plate_thickness = 8.0   # R3
hole_diameter = 11.0     # R4
hole_pattern_x = 63.0    # R5
hole_pattern_y = 38.0    # R5
edge_margin = 11.5       # R6

# Calculate the starting point for the hole pattern
start_x = (overall_length / 2) - (hole_pattern_x / 2) - edge_margin
start_y = (overall_width / 2) - (hole_pattern_y / 2) - edge_margin

# Create the base plate
plate = cq.Workplane("XY").box(overall_length, overall_width, plate_thickness)

# Create the holes
result = plate.faces(">Z").workplane().hole(hole_diameter)
for i in range(2):
    for j in range(2):
        result = result.faces(">Z").workplane(centerOption="CenterOfBoundBox").transformed(offset=(start_x + i * hole_pattern_x, start_y + j * hole_pattern_y, 0)).hole(hole_diameter)

# Bind the finished part to the variable `result`
result = result.val()
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 15. X and Y swapped
- model: openai/gpt-4.1-nano  tier: L3  spec: gen-0169  run: 20260924T031430Z
- spec: plate 51.0 x 70.0 x 4.5, hole 8.5, pitch 37.0 x 56.0, margin 7.0
- correct hole centres: (+/-18.5, +/-28)
- plate position: X -35 .. 35   Y -25.5 .. 25.5   Z -2.25 .. 2.25
- measured hole centres: [(-28.0, -18.5), (-28.0, 18.5), (28.0, -18.5), (28.0, 18.5)]

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 51 x 70 x 4.5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.25 mm. Each bore axis lies 7 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define parameters
envelope_length = 70
envelope_width = 51
envelope_height = 4.5

bore_radius = 4.25
bore_offset_x = 7
bore_offset_y = 7

# Create the main envelope
result = (
    cq.Workplane("XY")
    .box(envelope_length, envelope_width, envelope_height)
    .translate((0, 0, 0))
)

# Calculate positions for the four bores
# Bores are centered along the width and length, offset from edges
bore_positions = [
    (bore_offset_x, bore_offset_y),
    (envelope_length - bore_offset_x, bore_offset_y),
    (bore_offset_x, envelope_width - bore_offset_y),
    (envelope_length - bore_offset_x, envelope_width - bore_offset_y),
]

# Subtract the four through bores
for x, y in bore_positions:
    result = result.faces(">Z").workplane().pushPoints([(x - envelope_length/2, y - envelope_width/2)]).hole(2 * bore_radius)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 16. no part produced
- model: qwen/qwen3-coder-next  tier: L3  spec: gen-0149  run: 20260924T011250Z
- spec: plate 43.0 x 76.5 x 5.0, hole 9.5, pitch 17.0 x 50.5, margin 13.0
- correct hole centres: (+/-8.5, +/-25.25)
- error: `result` holds no CadQuery shape (got Workplane)

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 43 x 76.5 x 5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.75 mm. Each bore axis lies 13 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Part dimensions
envelope_x = 43.0
envelope_y = 76.5
envelope_z = 5.0

# Bore radius
bore_radius = 4.75

# Distance from closest outer edges to bore centers
edge_distance = 13.0

# Calculate bore center positions
# Since bores are 13 mm from the two closest outer edges, and the part is centered at (0,0,0),
# the bore centers will be at:
# x = ±(envelope_x/2 - edge_distance)
# y = ±(envelope_y/2 - edge_distance)

bore_x_positions = [-(envelope_x/2 - edge_distance), envelope_x/2 - edge_distance]
bore_y_positions = [-(envelope_y/2 - edge_distance), envelope_y/2 - edge_distance]

# Create the base block centered at origin
result = cq.Workplane("XY").box(envelope_x, envelope_y, envelope_z).faces(">Z").workplane().center(0, 0)

# Add all 4 bores
for x in bore_x_positions:
    for y in bore_y_positions:
        result = result.workplane().center(x, y).circle(bore_radius).extrude(-envelope_z, combine=True)
        # Move back to the main workplane center for next bore
        result = result.workplane().center(-x, -y)

# Ensure result is centered at origin (already is by construction)
result = result.translate((0, 0, 0))
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 17. some holes right, some wrong
- model: qwen/qwen3-coder-30b-a3b-instruct  tier: L1  spec: gen-0053  run: 20260924T004105Z
- spec: plate 50.0 x 61.5 x 4.0, hole 4.0, pitch 9.0 x 20.5, margin 20.5
- correct hole centres: (+/-4.5, +/-10.25)
- plate position: X -25 .. 25   Y -30.75 .. 30.75   Z -2 .. 2
- measured hole centres: [(-4.5, -10.25), (0.0, -20.5), (0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        50.0 mm
  R2  Overall width (Y):         61.5 mm
  R3  Plate thickness (Z):       4.0 mm
  R4  Fastener holes:            4 off, through, 4.0 mm diameter
  R5  Hole pattern:              rectangular, 9.0 mm x 20.5 mm centres
  R6  Edge margin:               20.5 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Dimensions
length = 50.0
width = 61.5
thickness = 4.0
hole_diameter = 4.0
hole_pattern_x = 9.0
hole_pattern_y = 20.5
edge_margin = 20.5

# Create the base plate
result = cq.Workplane("XY").box(length, width, thickness)

# Create and remove the fastener holes
# Calculate hole positions based on the hole pattern and edge margin
hole_x = (length - 2 * edge_margin) / 2
hole_y = (width - 2 * edge_margin) / 2

# Create holes at the four corners
hole_positions = [
    (-hole_x, -hole_y),
    (hole_x, -hole_y),
    (-hole_x, hole_y),
    (hole_x, hole_y)
]

for x, y in hole_positions:
    result = result.faces(">Z").workplane().center(x, y).hole(hole_diameter)

result = result.val()
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 18. CadQuery API error
- model: google/gemma-3-4b-it  tier: L3  spec: gen-0149  run: 20260924T145836816612Z-9a9e12
- spec: plate 43.0 x 76.5 x 5.0, hole 9.5, pitch 17.0 x 50.5, margin 13.0
- correct hole centres: (+/-8.5, +/-25.25)
- API error kind: no such method: Workplane.with_tolerance
- error: execution failed [raised in model code]: AttributeError: 'Workplane' object has no attribute 'with_tolerance'

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 43 x 76.5 x 5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.75 mm. Each bore axis lies 13 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

result = cq.Workplane("XY") \
    .box(43, 76.5, 5) \
    .faces(">Z") \
    .extrude(13) \
    .faces(">Z") \
    .fillet(4.75) \
    .translate((0, 0, 0)) \
    .with_tolerance(0.1)
for i in range(4):
    result = result.makeRib([])
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 19. change not propagated to pitch
- model: google/gemma-3-27b-it  tier: L4  spec: gen-0021  run: 20260924T021321Z
- spec: plate 84.0 x 34.5 x 6.5, hole 4.0, pitch 60.0 x 10.5, margin 12.0
- correct hole centres: (+/-30, +/-5.25)
- rev A (before the change): plate 84.0 x 34.5 x 6.0, hole 4.0, pitch 64.0 x 14.5, margin 10.0

<details><summary>full prompt</summary>

````
Below is the current CadQuery model of a mounting plate (rev A).

```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(84.0, 34.5, 6.0)
    .faces(">Z").workplane()
    .rect(64.0, 14.5, forConstruction=True)
    .vertices()
    .hole(4.0)
)
```

ENGINEERING CHANGE ORDER, rev A -> rev B:
  - change plate thickness (Z) from 6 mm to 6.5 mm
  - change edge margin (hole centre to nearest edges) from 10 mm to 12 mm
Every other characteristic stays exactly as in rev A: four through holes, one per corner, each hole centre at the stated edge margin from its two nearest edges, part centred on the origin.

Return the complete rev B CadQuery module with the part bound to `result`.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(84.0, 34.5, 6.5)
    .faces(">Z").workplane()
    .rect(64.0, 14.5, forConstruction=True)
    .vertices()
    .hole(4.0)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 20. holes stacked at one point
- model: meta-llama/llama-3.3-70b-instruct  tier: L2  spec: gen-0169  run: 20260924T014436Z
- spec: plate 51.0 x 70.0 x 4.5, hole 8.5, pitch 37.0 x 56.0, margin 7.0
- correct hole centres: (+/-18.5, +/-28)
- plate position: X -25.5 .. 25.5   Y -35 .. 35   Z 0 .. 4.5
- measured hole centres: [(0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        51.0 mm
  R2  Overall width (Y):         70.0 mm
  R3  Plate thickness (Z):       4.5 mm
  R4  Fastener holes:            4 off, through, 8.5 mm diameter
  R5  Hole pattern:              rectangular, one hole near each corner
  R6  Edge margin:               7.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions
length = 51.0
width = 70.0
thickness = 4.5
hole_diameter = 8.5
edge_margin = 7.0

# Calculate the hole positions
hole_x = length / 2 - edge_margin
hole_y = width / 2 - edge_margin

# Create the plate
result = (
    cq.Workplane("XY")
   .rect(length, width)
   .extrude(thickness)
)

# Create the fastener holes
result = (
    result
   .faces(">Z")
   .hole(hole_diameter / 2)
   .faces("<Z")
   .hole(hole_diameter / 2)
)

# Create the hole pattern
result = (
    result
   .faces(">Z")
   .hole(hole_diameter / 2)
   .translate((hole_x, hole_y, 0))
   .hole(hole_diameter / 2)
   .translate((-hole_x, hole_y, 0))
   .hole(hole_diameter / 2)
   .translate((-hole_x, -hole_y, 0))
   .hole(hole_diameter / 2)
   .translate((hole_x, -hole_y, 0))
   .hole(hole_diameter / 2)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 21. some holes right, some wrong
- model: microsoft/phi-4  tier: L2  spec: gen-0015  run: 20260924T030042Z
- spec: plate 178.5 x 152.5 x 11.0, hole 6.5, pitch 142.5 x 116.5, margin 18.0
- correct hole centres: (+/-71.25, +/-58.25)
- plate position: X -89.25 .. 89.25   Y -76.25 .. 76.25   Z -5.5 .. 5.5
- measured hole centres: [(-71.25, -58.25), (0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        178.5 mm
  R2  Overall width (Y):         152.5 mm
  R3  Plate thickness (Z):       11.0 mm
  R4  Fastener holes:            4 off, through, 6.5 mm diameter
  R5  Hole pattern:              rectangular, one hole near each corner
  R6  Edge margin:               18.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions
length = 178.5  # R1
width = 152.5   # R2
thickness = 11.0 # R3
hole_diameter = 6.5 # R4
edge_margin = 18.0 # R6

# Create the base plate
plate = cq.Workplane("XY").box(length, width, thickness)

# Calculate hole positions
hole_positions = [
    (-length/2 + edge_margin, -width/2 + edge_margin),
    (-length/2 + edge_margin, width/2 - edge_margin),
    (length/2 - edge_margin, -width/2 + edge_margin),
    (length/2 - edge_margin, width/2 - edge_margin)
]

# Cut the holes
for pos in hole_positions:
    plate = plate.faces(">Z").workplane().center(pos[0], pos[1]).hole(hole_diameter)

# Bind the finished part to the result variable
result = plate
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 22. no holes
- model: qwen/qwen3-coder-30b-a3b-instruct  tier: L3  spec: gen-0057  run: 20260924T004105Z
- spec: plate 214.5 x 126.5 x 7.0, hole 4.5, pitch 175.5 x 87.5, margin 19.5
- correct hole centres: (+/-87.75, +/-43.75)
- plate position: X -214.5 .. 0   Y -126.5 .. 0   Z -0 .. 7
- measured hole centres: []

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 7 mm plate cut to 126.5 mm wide (Y) and 214.5 mm long (X). It needs four 4.5 mm holes straight through, spaced 175.5 mm apart along the length and 87.5 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Plate dimensions
plate_width = 126.5  # Y direction
plate_length = 214.5  # X direction
hole_diameter = 4.5
hole_spacing_x = 175.5
hole_spacing_y = 87.5

# Create the base plate
result = cq.Workplane("XY").rect(plate_length, plate_width).extrude(7)

# Calculate hole positions (centered on plate)
hole_offset_x = hole_spacing_x / 2
hole_offset_y = hole_spacing_y / 2

# Add the four holes
result = result.faces(">X").workplane().center(-hole_offset_x, -hole_offset_y).hole(hole_diameter)
result = result.faces(">X").workplane().center(hole_offset_x, -hole_offset_y).hole(hole_diameter)
result = result.faces(">X").workplane().center(-hole_offset_x, hole_offset_y).hole(hole_diameter)
result = result.faces(">X").workplane().center(hole_offset_x, hole_offset_y).hole(hole_diameter)

# Ensure the plate is centered on the origin
result = result.translate((-plate_length/2, -plate_width/2, 0))
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 23. holes stacked at one point
- model: google/gemma-3-27b-it  tier: L1  spec: gen-0166  run: 20260924T021321Z
- spec: plate 124.0 x 95.5 x 14.0, hole 11.0, pitch 100.0 x 71.5, margin 12.0
- correct hole centres: (+/-50, +/-35.75)
- plate position: X -62 .. 62   Y -47.75 .. 47.75   Z -7 .. 7
- measured hole centres: [(0.0, 0.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        124.0 mm
  R2  Overall width (Y):         95.5 mm
  R3  Plate thickness (Z):       14.0 mm
  R4  Fastener holes:            4 off, through, 11.0 mm diameter
  R5  Hole pattern:              rectangular, 100.0 mm x 71.5 mm centres
  R6  Edge margin:               12.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

result = (
    cq.Workplane("XY")
    .box(124.0, 95.5, 14.0)
    .faces(">Z")
    .workplane()
    .center(0, 0)
    .hole(11.0)
    .translate((50.0, 35.75, 0))
    .hole(11.0)
    .translate((-50.0, 35.75, 0))
    .hole(11.0)
    .translate((-50.0, -35.75, 0))
    .hole(11.0)
    .translate((50.0, -35.75, 0))
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 24. off Z datum
- model: meta-llama/llama-4-maverick  tier: L2  spec: gen-0169  run: 20260924T032942Z
- spec: plate 51.0 x 70.0 x 4.5, hole 8.5, pitch 37.0 x 56.0, margin 7.0
- correct hole centres: (+/-18.5, +/-28)
- plate position: X -25.5 .. 25.5   Y -35 .. 35   Z 0 .. 4.5
- measured hole centres: [(-18.5, -28.0), (-18.5, 28.0), (18.5, -28.0), (18.5, 28.0)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        51.0 mm
  R2  Overall width (Y):         70.0 mm
  R3  Plate thickness (Z):       4.5 mm
  R4  Fastener holes:            4 off, through, 8.5 mm diameter
  R5  Hole pattern:              rectangular, one hole near each corner
  R6  Edge margin:               7.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions
length = 51.0  # R1
width = 70.0   # R2
thickness = 4.5  # R3
hole_diameter = 8.5  # R4
edge_margin = 7.0  # R6

# Calculate the hole positions
hole_x = (length / 2) - edge_margin
hole_y = (width / 2) - edge_margin

# Create the plate
result = (cq.Workplane("XY")
          .box(length, width, thickness, centered=(True, True, False))
          .faces(">Z").workplane().center(0, 0).tag("mid")
          .workplaneFromTagged("mid")
          .pushPoints([(-hole_x, -hole_y), (hole_x, -hole_y), (-hole_x, hole_y), (hole_x, hole_y)])
          .hole(hole_diameter)
          )
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 25. some holes right, some wrong
- model: qwen/qwen3-30b-a3b-instruct-2507  tier: L1  spec: gen-0004  run: 20260924T002523Z
- spec: plate 209.5 x 143.5 x 5.0, hole 10.5, pitch 175.5 x 109.5, margin 17.0
- correct hole centres: (+/-87.75, +/-54.75)
- plate position: X -104.75 .. 104.75   Y -71.75 .. 71.75   Z 0 .. 5
- measured hole centres: [(-87.75, 54.75), (0.0, 0.0), (87.75, 54.75)]

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        209.5 mm
  R2  Overall width (Y):         143.5 mm
  R3  Plate thickness (Z):       5.0 mm
  R4  Fastener holes:            4 off, through, 10.5 mm diameter
  R5  Hole pattern:              rectangular, 175.5 mm x 109.5 mm centres
  R6  Edge margin:               17.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define dimensions from requirements
length = 209.5  # R1: Overall length (X)
width = 143.5   # R2: Overall width (Y)
thickness = 5.0  # R3: Plate thickness (Z)
hole_diameter = 10.5  # R4: Fastener hole diameter
hole_pattern_length = 175.5  # R5: Hole pattern length (X direction)
hole_pattern_width = 109.5   # R5: Hole pattern width (Y direction)
edge_margin = 17.0  # R6: Margin from hole center to nearest edge

# Calculate hole positions based on edge margin and pattern size
# Center of the plate is at origin, so holes are offset from center
hole_x_offset = hole_pattern_length / 2
hole_y_offset = hole_pattern_width / 2

# Create the base plate centered at origin
plate = cq.Workplane("XY").box(length, width, thickness, centered=(True, True, False))

# Define the four hole centers
# Holes are arranged in a rectangle with specified pattern and margin
# The hole centers are located at:
# (±hole_x_offset, ±hole_y_offset) relative to the center
hole_centers = [
    (hole_x_offset, hole_y_offset),
    (-hole_x_offset, hole_y_offset),
    (-hole_x_offset, -hole_y_offset),
    (hole_x_offset, -hole_y_offset)
]

# Add the four through holes
result = plate
for x, y in hole_centers:
    result = result.faces(">Z").workplane().center(x, y).hole(hole_diameter)

# Ensure the final result is properly bound
result = result
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 26. geometry kernel failure
- model: qwen2.5-coder:1.5b  tier: L3  spec: gen-0012  run: 20260923T164833Z
- spec: plate 66.0 x 124.0 x 9.0, hole 10.5, pitch 46.0 x 104.0, margin 10.0
- correct hole centres: (+/-23, +/-52)
- error: execution failed [raised in cadquery: Solid.makeBox]: Standard_DomainError: 

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 66 x 124 x 9 mm, centred at (0, 0, 0). Qty 4 through bores of radius 5.25 mm. Each bore axis lies 10 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions of the part
l, w, h = 66, 124, 9

# Create the workplane
workplane = cq.Workplane("XY")

# Create the outer box
outer_box = workplane.box(l, w, h)

# Create the inner box
inner_box = workplane.box(l - 2 * 10, w - 2 * 10, h - 2 * 10)

# Create the bores
bores = []
for i in range(4):
    bore = workplane.circle(5.25).extrude(10)
    bores.append(bore)

# Combine the outer box, inner box, and bores
result = outer_box - inner_box - bores

# Display the result
cq.exporters.export(result, "part.stl")
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 27. change not propagated to pitch
- model: qwen/qwen3-coder-next  tier: L4  spec: gen-0041  run: 20260924T011250Z
- spec: plate 86.0 x 61.0 x 8.0, hole 11.0, pitch 63.0 x 38.0, margin 11.5
- correct hole centres: (+/-31.5, +/-19)
- rev A (before the change): plate 94.5 x 61.0 x 9.5, hole 11.0, pitch 71.5 x 38.0, margin 11.5

<details><summary>full prompt</summary>

````
Below is the current CadQuery model of a mounting plate (rev A).

```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(94.5, 61.0, 9.5)
    .faces(">Z").workplane()
    .rect(71.5, 38.0, forConstruction=True)
    .vertices()
    .hole(11.0)
)
```

ENGINEERING CHANGE ORDER, rev A -> rev B:
  - change overall length (X) from 94.5 mm to 86 mm
  - change plate thickness (Z) from 9.5 mm to 8 mm
Every other characteristic stays exactly as in rev A: four through holes, one per corner, each hole centre at the stated edge margin from its two nearest edges, part centred on the origin.

Return the complete rev B CadQuery module with the part bound to `result`.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq
result = (
    cq.Workplane("XY")
    .box(86.0, 61.0, 8.0)
    .faces(">Z").workplane()
    .rect(71.5, 38.0, forConstruction=True)
    .vertices()
    .hole(11.0)
)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 28. holes stacked at one point
- model: microsoft/phi-4  tier: L3  spec: gen-0169  run: 20260924T074549092729Z-aa3b44
- spec: plate 51.0 x 70.0 x 4.5, hole 8.5, pitch 37.0 x 56.0, margin 7.0
- correct hole centres: (+/-18.5, +/-28)
- plate position: X -25.5 .. 25.5   Y -35 .. 35   Z -2.25 .. 2.25
- measured hole centres: [(-0.0, -0.0)]

<details><summary>full prompt</summary>

````
Inspection criteria for part to be modelled in CadQuery (bind to `result`): envelope 51 x 70 x 4.5 mm, centred at (0, 0, 0). Qty 4 through bores of radius 4.25 mm. Each bore axis lies 7 mm from the two closest outer edges. No other features.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the envelope dimensions
length = 51
width = 70
height = 4.5

# Define bore parameters
bore_radius = 4.25
bore_distance_from_edge = 7

# Create the base box
base = cq.Workplane("XY").box(length, width, height)

# Calculate bore positions
bore_positions = [
    (-length / 2 + bore_distance_from_edge, -width / 2 + bore_distance_from_edge, 0),
    (-length / 2 + bore_distance_from_edge, width / 2 - bore_distance_from_edge, 0),
    (length / 2 - bore_distance_from_edge, -width / 2 + bore_distance_from_edge, 0),
    (length / 2 - bore_distance_from_edge, width / 2 - bore_distance_from_edge, 0)
]

# Create bores
for pos in bore_positions:
    base = base.faces(">Z").workplane(centerOption="CenterOfMass").hole(bore_radius * 2)

# Bind the result
result = base
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 29. CadQuery API error
- model: meta-llama/llama-3.3-70b-instruct  tier: L1  spec: gen-0032  run: 20260924T014436Z
- spec: plate 214.0 x 150.0 x 10.5, hole 9.0, pitch 198.0 x 134.0, margin 8.0
- correct hole centres: (+/-99, +/-67)
- API error kind: no such method: Workplane.push
- error: execution failed [raised in model code]: AttributeError: 'Workplane' object has no attribute 'push'

<details><summary>full prompt</summary>

````
Write a CadQuery Python module that builds the part below and binds the
finished solid to a variable named `result`.

REQUIREMENTS
  R1  Overall length (X):        214.0 mm
  R2  Overall width (Y):         150.0 mm
  R3  Plate thickness (Z):       10.5 mm
  R4  Fastener holes:            4 off, through, 9.0 mm diameter
  R5  Hole pattern:              rectangular, 198.0 mm x 134.0 mm centres
  R6  Edge margin:               8.0 mm from hole centre to each nearest edge

The plate is centred on the origin with its thickness along Z.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions
length = 214.0
width = 150.0
thickness = 10.5
hole_diameter = 9.0
hole_pitch_x = 198.0
hole_pitch_y = 134.0
edge_margin = 8.0

# Calculate the hole positions
hole_x = hole_pitch_x / 2
hole_y = hole_pitch_y / 2

# Create the plate
result = (
    cq.Workplane("XY")
   .rect(length, width)
   .extrude(thickness)
)

# Create the fastener holes
holes = (
    cq.Workplane("XY")
   .push(
        [
            (-hole_x, -hole_y),
            (-hole_x, hole_y),
            (hole_x, -hole_y),
            (hole_x, hole_y),
        ]
    )
   .circle(hole_diameter / 2)
   .extrude(-thickness)
)

# Cut the holes from the plate
result = result.cut(holes)
```
````
</details>

- [ ] agree   - [ ] disagree   note:

## 30. CadQuery API error
- model: qwen2.5-coder:1.5b  tier: L3  spec: gen-0028  run: 20260923T164833Z
- spec: plate 70.0 x 47.5 x 5.5, hole 8.5, pitch 37.0 x 14.5, margin 16.5
- correct hole centres: (+/-18.5, +/-7.25)
- API error kind: operation needs a solid on the stack
- error: execution failed [raised in cadquery: Workplane.findSolid]: ValueError: Cannot find a solid on the stack or in the parent chain

<details><summary>full prompt</summary>

````
Hi, could you knock up the adapter plate in CadQuery? Stock is 5.5 mm plate cut to 47.5 mm wide (Y) and 70 mm long (X). It needs four 8.5 mm holes straight through, spaced 37 mm apart along the length and 14.5 mm apart across the width, pattern centred on the plate, plate centred on the origin. Please leave the finished body in a variable called result.
````
</details>

<details><summary>full code</summary>

````
```python
import cadquery as cq

# Define the dimensions of the plate
l = 70  # Length
w = 47.5  # Width
h = 5.5  # Height

# Create the plate
plate = cq.Workplane("XY").box(l, w, h)

# Define the hole pattern
hole_pattern = cq.Workplane("XY").hole(8.5, 37, 14.5)

# Apply the hole pattern to the plate
result = plate.cut(hole_pattern)

# Display the result
cq.exporters.export(result, "adapter_plate.stl")
```
````
</details>

- [ ] agree   - [ ] disagree   note:
